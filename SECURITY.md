# 安全策略

## 支持的版本

本库处于早期阶段，仅对最新发布版本提供安全更新。

| 版本 | 支持 |
| --- | --- |
| 最新 release | ✅ |
| 更早版本 | ❌ |

## 报告安全漏洞

**请勿通过公开 Issue 报告安全漏洞。**

推荐使用 GitHub 的私密报告通道：

1. 打开仓库的 **Security** 标签页
2. 点击 **Report a vulnerability**
3. 填写复现步骤与影响说明

如果你无法使用该通道，也可以直接发送邮件至 `github@fanshuhua.com`。

### 请在报告中包含

- 受影响的版本或提交
- 最小复现步骤（越简洁越好）
- 可能的影响范围
- 若有修复建议，欢迎一并说明

### 处理流程

- 我们会在 **7 天内**确认收到报告
- 确认漏洞后会在修复发布前与你沟通披露时间
- 修复完成后，会在 Release Notes 中致谢（除非你希望匿名）

## 安全模型说明

为便于评估风险，以下是本库的信任边界：

- 本库是**进程内的插件运行时**，不涉及网络通信、不执行序列化/反序列化、不访问文件系统
- 插件以普通 Python 对象运行，**拥有调用进程的全部权限**——本库不提供插件沙箱，
  不应将不受信任的代码作为插件加载
- 配置校验通过用户提供的 `Config` Schema 完成，Schema 本身的正确性由用户负责
